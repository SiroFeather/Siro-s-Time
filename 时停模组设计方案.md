# 时停（Time Stop）模组设计方案 — Forge 1.20.1

> 目标：添加一个物品，玩家手持右键触发/解除"时停"；时停期间除使用者外所有实体的运动、事件、判定、AI 全部停止；多人模式下冻结其他玩家（不能移动、使用物品、攻击）；时停解除后 0.5 秒内移除原版受伤无敌帧；支持实体白名单；与车万女仆（Touhou Little Maid）的女仆实体适配。

---

## 1. 技术选型

| 项目 | 选择 |
|---|---|
| MC 版本 | 1.20.1 |
| 加载器 | Forge `1.20.1-47.2.0` |
| 映射 | 官方映射（Mojang Official） |
| Java | 17 |
| 构建 | ForgeGradle 6.0 + Gradle 8.x |
| 依赖 | 仅 Forge 本体；车万女仆为**软依赖**（可选，检测到才启用适配） |
| 网络 | 自有 `SimpleChannel`（用于客户端同步时停状态/特效） |

---

## 2. 整体架构

```
src/main/java/com/example/timestop/
├── TimeStopMod.java            # @Mod 主类，注册总线、DeferredRegister
├── item/
│   └── TimeStopItem.java       # 时停物品（右键切换）
├── timesstop/                  # 时停核心逻辑（服务端权威）
│   ├── TimeStopState.java      # 时停状态机（单例，仅服务端使用）
│   ├── TimeStopFreezer.java    # 冻结逻辑（ServerTick + 实体快照）
│   └── TimeStopEvents.java     # 各类 Forge 事件处理器（拦截交互/受伤/AI）
├── config/
│   └── TimeStopConfig.java     # ForgeConfigSpec：白名单、时长、无敌帧窗口等
├── compat/
│   └── TouhouMaidCompat.java   # 车万女仆软依赖适配（默认白名单女仆）
├── network/
│   ├── ModNetworking.java      # SimpleChannel 注册
│   └── TimeStopStatePacket.java# 服务端->客户端时停状态同步（特效/UI）
└── client/                     # 仅客户端
    └── ClientTimeStopFX.java   # 时停视觉特效（可选项）
```

关键原则：
- **一切时停逻辑都在服务端裁决**，客户端只做表现（画面效果/提示）。
- 玩家右键只是"发出切换请求"，服务端校验并改变状态。
- 冻结采用"**每 tick 恢复快照 + 零速度 + 取消 tick/AI**"的组合，可靠性最高。

---

## 3. 物品注册（TimeStopItem）

- 使用 `DeferredRegister<Item>` 注册 `time_stop_item`（例如"时停怀表 / The World"）。
- 注册到 `CreativeModeTab`，并设置 `maxStackSize(1)`。
- 重写 `use(Level, Player, InteractionHand)`（右键）：
  1. 仅服务端执行；若玩家不在 `ServerLevel` 直接返回 `PASS`。
  2. 调用 `TimeStopState.toggle(server, player)`：
     - 未激活 → 激活：记录 `stopperUUID`、捕获快照。
     - 已激活且 `stopperUUID == 玩家` → 解除：启动 0.5s 无敌帧移除窗口。
     - 已激活但请求者不是停时者 → 拒绝（其他玩家本身也被冻结，正常情况下无法触发）。
  3. 返回 `InteractionResult.sidedSuccess(...)`，并播放切换音效/粒子。
- 工具提示（Tooltip）显示当前状态（"时停进行中 / 已解除"），使用 NBT 或客户端包缓存。

---

## 4. 时停状态机（TimeStopState）

全局单例（服务端），字段：

```java
public class TimeStopState {
    private static boolean active = false;
    private static UUID stopperUUID;
    private static int invulnWindowTicks = 0;      // 解除后剩余的无敌帧移除窗口
    private static final Map<UUID, FrozenSnapshot> snapshots = new HashMap<>();
    // ...
}
```

- `FrozenSnapshot`：记录实体冻结瞬间/上一 tick 的 `Vec3 pos`、`float yRot, xRot`、`Vec3 motion`。
- 状态切换：
  - `activate(server, player)`：置 `active=true`，存 stopper；对所有需要冻结的实体抓快照；广播状态包。
  - `deactivate(server, player)`：置 `active=false`，清空快照，置 `invulnWindowTicks = 10`（0.5s），广播状态包。
  - 服务端 tick 中递减 `invulnWindowTicks`，到 0 停止。
- 重置：服务端停止（如所有玩家下线 / 世界关闭）时复位。

---

## 5. 冻结核心逻辑（TimeStopFreezer）

在 `TickEvent.ServerTickEvent`（Phase.START）中执行：

1. 遍历所有 `ServerLevel` 的 `getAllEntities()`。
2. 判定"是否冻结该实体"：
   - 是使用者本人 → 不冻结（可自由移动/攻击/使用物品）。
   - 实体类型在**白名单**内 → 不冻结。
   - 其余一律冻结。
3. **Living 实体（主机制 = 取消 tick，不做位置复位）**：
   ```java
   // 服务端：取消 tick 本身即完全静止（事件在 tick() 最开头、super.tick() 之前触发，
   // 重力/移动/AI 全在后面 aiStep()，取消了就不会执行 → 位置根本不变，无抽动）
   // LivingTickEvent 处理器里：if (应冻结) event.setCanceled(true);
   // 这里只需清掉遗留速度，防止解除瞬间"残留速度"导致位移
   if (entity instanceof LivingEntity living) living.setDeltaMovement(Vec3.ZERO);
   if (entity instanceof Mob mob) { mob.setJumping(false); } // 防御性
   ```
4. **非 Living 实体 + 受外部推力实体的兜底复位**（箭/船/矿车/掉落物/经验球，以及被水流/活塞/停时者碰撞推动的实体）：
   ```java
   // 仅当 tick 无法覆盖（非 living）或被外力推动时才做快照复位
   FrozenSnapshot s = snapshots.computeIfAbsent(uuid, k -> capture(entity));
   entity.setDeltaMovement(Vec3.ZERO);
   entity.moveTo(s.pos.x, s.pos.y, s.pos.z, s.yRot, s.xRot);
   ```
   > 注意：对 living 实体不要"每 tick 复位位置"——这会和客户端插值打架造成视觉抽动；只有被持续外力推动时才有必要，且这是期望行为（"冻住的东西被推回去"）。
5. 同时注册 `LivingEvent.LivingTickEvent`：若处于时停且实体非豁免 → `event.setCanceled(true)`，直接阻断其 tick 更新（AI、重力、判定、受伤恢复等大部分逻辑随之失效）。
6. 对冻结的玩家额外处理：服务端取消其 tick + 拦截交互；**同时发包让客户端清空其移动输入**（见第 6 节），否则该玩家自己客户端会"走一步被拉回一步"。

> 说明：由于所有实体的 tick 都被取消 + 非 living 实体被复位，项目符号不会继续飞行、怪物不会追击、伤害/事件不会推进，实现"停止运动、事件、判定、AI"。

---

## 6. 多人模式：冻结其他玩家

服务端权威地：
- 其他玩家的 `LivingTickEvent` 被取消 + 每 tick 复位位置 → **无法移动**。
- 拦截以下事件（`setCanceled(true)`），使**无法使用物品/攻击**：
  - `PlayerInteractEvent.RightClickItem`
  - `PlayerInteractEvent.RightClickBlock`
  - `PlayerInteractEvent.LeftClickBlock`
  - `PlayerInteractEvent.EntityInteract`
  - `PlayerInteractEvent.EntityInteractSpecific`
  - `AttackEntityEvent`
  - `ProjectileImpactEvent`（若需禁用抛射物判定）
- 判断条件统一封装：`TimeStopState.isFrozen(player)`（时停中 且 非白名单 且 非停时者）。
- 客户端表现：通过 `TimeStopStatePacket` 通知其他玩家"你被时停"，客户端**清零移动输入**（`xxa/zza`、跳跃、疾跑），并禁用物品使用/攻击键 → 该玩家客户端不再"尝试移动"，因此不会有"被服务端拉回"的橡皮筋抖动；同时显示冻结效果（可选，纯表现）。

> 防抽动要点（已验证）：Forge 1.20.1 的 `LivingTickEvent` 在 `LivingEntity.tick()` 第一行、`super.tick()` 之前触发，取消后 gravity/move/AI（都在 `aiStep()`）全部不执行。因此**服务端冻结 living 实体靠"取消 tick"即可干净静止，不要叠加每 tick 复位位置**；复位只兜底非 living 实体与被外力推动的实体。被冻结玩家的橡皮筋来自其客户端本地预测，靠"客户端清输入 + 不重发位置包"消除；被冻结实体"还在动动画"靠"客户端也取消非豁免实体的 tick"消除。

---

## 7. 时停全程 + 结束后 0.5s 移除受伤无敌帧

- 无敌帧移除窗口 = **整个时停期间** + **时停结束后** `invulnWindowTicks`（默认 10 = 0.5 秒）。
- 实现：`active` 为真时每 tick 清零；解除后启动 `invulnWindowTicks` 收尾窗口，期间同样清零。
- 在 `ServerTickEvent`（Phase.END）中，对**所有可受伤实体**执行：
    ```java
    if (entity instanceof LivingEntity living) {
        living.invulnerableTime = 0;   // 原版受伤无敌帧清零
        living.hurtTime = 0;           // 可选：同时取消红色受伤动画
    }
    ```
- 效果：时停期间与解除后 0.5 秒内，连续攻击每次都造成伤害（无 10 tick 无敌间隔），便于打出连击。
- 若需要更精确，也可在 `LivingIncomingDamageEvent` 前把 `invulnerableTime` 清零，或注册该事件直接清零。二选一，tick 清零更稳妥。

---

## 8. 白名单配置（TimeStopConfig）

使用 `ModConfigSpec`，支持**实体注册名**列表，可在 config 中追加：

```toml
[timesstop]
# 时停时不冻结的实体 ID，可用 * 通配 modid
whitelist = [
  "minecraft:zombie",
  "touhou_little_maid:maid"
]
```

- 解析时缓存为 `Set<EntityType<?>>`（`BuiltInRegistries.ENTITY_TYPE` 查询）。
- 支持 `modid:*` 通配（可选增强）。
- 白名单同时作用于冻结判定与交互拦截判定。

---

## 9. 外部模组软依赖适配

- **软依赖**：运行时通过 `ModList.get().isLoaded("touhou_little_maid")` 检测，未安装则完全不触碰其类，避免硬崩溃。
- 适配内容：
  1. **默认白名单**：模组存在时，把女仆实体 ID（`touhou_little_maid:maid`，及可能的子类型）自动加入白名单默认值，使女仆不受时停影响，可继续执行女仆任务（工作/跟随/战斗 AI）。
  2. **可选增强**：若希望女仆"时停中依然能跟随主人"，可额外在 `TimeStopFreezer` 中特判女仆实体并保留其导航跟随；通过 config 开关 `maidFollowDuringTimeStop`（默认关）。
  3. 由于女仆本质是 `Mob`，若未在白名单，也会被通用冻结逻辑（取消 tick + 复位）正确停止，适配主要是"默认豁免 + 特性保留"。
- 通过 `@Mod.EventBusSubscriber` 或在 `TimeStopConfig` 初始化时用 `modIsLoaded` 判断注入默认值。


### 9.1 TACZ（Timeless and Classics: Zero）枪械模组适配

- **软依赖**：`TimeStopCompat.isTaczLoaded()` 检测 `tacz` 模组；`isTaczEntity()` 按命名空间识别其子弹等实体（不引用对方类）。
- **需求**：时停期间可开枪，子弹定在空中；时停解除后子弹**续飞并造成伤害**。
- **实现（通用机制，天然覆盖 TACZ 子弹）**：
  - 冻结时 `TimeStopState.captureMotion()` 记录每个被冻结实体的**原始速度**；
  - 时停期间照常清零速度（子弹/抛射物定住）；
  - 解除时 `restoreMotions()` 把记录的原始速度恢复到对应实体上 → 子弹/箭矢/掉落物续飞，命中后由原逻辑造成伤害；
  - 由配置 `resumeProjectilesOnRelease`（默认 true）控制。
- **生存时间暂停（已修复实测问题）**：Forge 1.20.1 无法跳过独立实体的服务端 tick（无 `EntityTickEvent`、`canUpdate` 仅对乘客生效、移除事件不可取消）。
  因此通过冻结 `tickCount`（年龄）实现：主循环 `tickNonPassenger` 先 `tickCount++` 再 `entity.tick()`，
  我们在每 tick START 把被冻结实体的 `tickCount` 钉回冻结时的值，使基于 `tickCount` 的自动消失检查永不触发 →
  时停中子弹不再"超时自然消失"，解除后从原年龄继续。
- **注意/局限**：
  - TACZ 未安装时不影响本模组；
  - 若 TACZ 子弹的生存时间使用**自定义字段**（而非 `tickCount`），该字段仍会推进，需 TACZ 自带时停支持或 CoreMod/Mixin；
  - 若 TACZ 子弹是纯服务端模拟（非实体），则本机制无法接管。



### 9.2 FTB Teams 兼容（同队豁免）

- **软依赖**：`TimeStopCompat.isFtbTeamsLoaded()` 检测 `ftbteams`；通过**反射**调用 `FTBTeamsAPI.api().getManager().getPlayerTeamFor(...).getMembers()` 获取队伍成员，不直接引用其类，未安装时无影响。
- **行为**：时停时自动豁免**与时停者同 FTB 队伍的玩家**及其**仆从/被驯服实体**（通过 `OwnableEntity.getOwnerUUID()` 或常见 NBT 归属字段 `OwnerUUID/Owner/Summoner/...` 解析归属者）。
- **同步**：FTB 同队成员 UUID 随 `TimeStopStatePacket` 下发客户端，客户端输入锁定/动画冻结同步豁免，多人下表现一致。
- 由配置 `ftbTeamFreezeExemption`（默认 true）控制。

### 9.3 战斗/伤害适配（含拔刀剑 SlashBlade 修复）

- **问题 A（连击无敌帧）**：Forge 1.20.1 的 `hurt()` 中 `invulnerableTime > 10` 且本次伤害 ≤ `lastHurt` 时会直接 `return false`（不造成伤害）。
  冻结实体不 tick，无敌帧不自然递减，之前仅 ServerTick END 清零 → 同一 tick 内 SlashBlade 等连击的后续命中全被挡掉。
  **修复**：新增 `LivingAttackEvent` 处理器（`hurt()` 最开头、无敌帧检查之前触发，且对非玩家目标必定触发）：
  时停生效期间清零受害者的 `invulnerableTime` / `hurtTime`，使每次命中（含同 tick 多段）都走满额伤害分支。
- **问题 B（召唤剑射线零长度）**：SlashBlade 的召唤剑/斩击实体是 `Projectile`，其 `tick()` 用 `getDeltaMovement()`
  作为命中射线长度（`position + deltaMovement` 做 raycast）。我们冻结时把非生物实体速度清零 → 射线长度为零 → 永远 MISS → 无法造成伤害。
  **修复（SlashBlade 适配）**：软依赖识别（`slashblade` 命名空间 + `instanceof Projectile` + 归属者）——
  **豁免"停时者本人（或 FTB 同队玩家）发射的 SlashBlade 投射类伤害实体"不被冻结**，使其按原逻辑飞行并命中造成伤害；
  客户端同步豁免，正常渲染飞行。归属者解析失败时也防御性豁免。
- **问题 C（右键连招后续段被挡）**：1.20.1 `hurt()` 在 `invulnerableTime>10` 且本次伤害 ≤ `lastHurt` 时直接 `return false`。
  连招段3+由 `releaseAction`（松开右键）触发、且冻结目标无击退，玩家不易松开追打；同时若某次清零未生效，后续段会被 `lastHurt` 门挡下。
  **修复**：多重清零加固——冻结生物每 tick 起点 + `LivingAttackEvent` + `LivingHurtEvent` 三处清零 `invulnerableTime`/`hurtTime`，
  确保连招每段（含同 tick 多段）都走满额伤害分支。

---

## 10. 客户端表现（可选）

- 时停期间：
  - **客户端冻结动画（生物）**：`ClientFreezer` 在客户端取消非豁免实体的 `LivingTickEvent`（`setCanceled(true)`），让它们停在当前姿势（走路摆动、挥臂、使用物品动画不再播放）。
  - **客户端冻结动画（非生物 / 方案 A）**：`ClientEntityFreezer` 在 `ClientTickEvent`(END) 遍历渲染实体，对非生物实体快照复位（位置/朝向/速度/`tickCount`），停住掉落物旋转、船/矿车摆动等按 tick 计时的动画。
  - **白名单同步**：`TimeStopStatePacket` 携带白名单列表，客户端 `ClientTimeStopState` 据此做豁免判定，与服务端一致。
  - **被冻结玩家本人**：清零其移动输入（`xxa/zza`、跳跃、疾跑），禁用交互键，防止客户端预测与服务端拉回产生橡皮筋抖动；不取消其自身 tick（避免影响相机）。
  - 屏幕叠加滤镜（青/黄绿色调 + 轻微网格/怀表贴图），模拟"The World"效果。
- 使用 `TimeStopStatePacket`（服务端 -> 客户端）同步 `active` 状态、停时者 UUID 与白名单。
- 全部为纯表现，不影响服务端逻辑。

---

## 11. 构建与测试要点

- `build.gradle`：
  - `minecraft { mappings channel='official', version='1.20.1' }`
  - `minecraft_version=1.20.1`、`forge_version=47.2.0`。
- 测试清单：
  - 单机：时停后怪物/动物/村民冻结；箭停在空中；使用者在时停中自由移动。
  - 多人（开服/局域网）：其他玩家无法移动、右键、攻击；只有停时者可解除。
  - 解除后 0.5s：用木剑/高攻速武器连续攻击，确认每次都掉血（无无敌帧）。
  - 白名单：加入 `minecraft:zombie` 后僵尸继续行动。
  - 女仆：装上车万女仆后，确认女仆默认不受时停影响；未装该模组时模组正常运行。

---

## 12. 需要留意的边界情况

- 停时者骑乘坐骑/矿车：坐骑默认被冻结，可能导致骑乘异常——需决定是否豁免"停时者的载具"（建议豁免）。
- 时停中停时者射出的箭会被冻结（符合"除使用者外所有实体"），可提示玩家注意。
- 若在时停中玩家打开容器/聊天等 GUI，属玩家自身行为，不受影响。
- 服务端重启/切换维度时确保重置状态与快照。
- 抛射物命中判定：如需"判定也停止"，可取消 `ProjectileImpactEvent`；否则保持默认（冻结后基本不会移动因此很少触发）。

---

## 13. 里程碑拆分

1. **M0 工程骨架**：ForgeGradle 初始化、主类、物品注册、CreativeTab、config 框架。 ✅ 已完成
2. **M1 单机核心**：右键切换 + ServerTick 冻结 + LivingTickEvent 取消。 ✅ 已完成
3. **M2 多人安全**：交互/攻击事件拦截 + 状态包同步 + 客户端表现。 ✅ 已完成
4. **M3 无敌帧窗口**：解除后 0.5s 清除 invulnerableTime。 ✅ 已完成
5. **M4 白名单 + 女仆适配**：config 白名单、软依赖检测、女仆默认豁免。 ✅ 已完成
6. **M5 打磨**：音效（时停开启使用 DIO 语音）/粒子/滤镜、Tooltip 状态、测试与发布。 ✅ 已完成
